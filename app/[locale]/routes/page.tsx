'use client';

import { RoutesPage } from '@/page-components/RoutesPage';
import { PageWrapper } from '@/components/PageWrapper';

export default function Routes() {
  return (
    <PageWrapper>
      <RoutesPage />
    </PageWrapper>
  );
}
